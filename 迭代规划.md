# Features
## 重要
[] 用户功能，简单的数据库实现。

## 不重要
[] 房间可以自由调整座位次序，即可以自己坐在空位置。房主可以把对应位置的ai剔除。游戏开局，以房间顺序开始
[] 动图不再使用emoji，能否生成一些图片，替换掉这些emoji
[] 人机加强版

# 用户功能
1. 看看能用什么数据库，例如sqllite这类轻量的数据库保存信息
2. 记录每次对局的结果，和操作记录。
3. 能够查看自己的对局记录
4. 支持5台设备，永久session
5. 需要考虑多台设备同一个账号同时进入一个对局，要踢出去只剩一个
6. 房主可以直接重开房间即所有人到房间的准备页面。（对局记录也记一半，不算胜负）
7. 个人可以单独退出房间，留下AI继续。但是如果只是用户断连是不能自动ai接管的。必须是用户点击某个按钮退出才算。并提示其他人（昵称上加上，或者有一个元素特殊标注）
8. 用户退出后可以单独进其他房间开始新对局。
